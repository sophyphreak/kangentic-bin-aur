# kangentic-bin AUR Maintenance

This repository maintains the `kangentic-bin` AUR package.

The GitHub Actions workflow checks the latest upstream Kangentic GitHub release,
updates `PKGBUILD` and `.SRCINFO` when a new release appears, and pushes the
updated package files to AUR.
